# Copy to .env and fill in before `docker compose up -d`.

# The hostname you registered and pointed at this server.
GUITHUB_DOMAIN=tabs.example.com

# Where Let's Encrypt sends certificate expiry warnings.
ACME_EMAIL=you@example.com
